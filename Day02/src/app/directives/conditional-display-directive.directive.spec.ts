import { ConditionalDisplayDirectiveDirective } from './conditional-display-directive.directive';

describe('ConditionalDisplayDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionalDisplayDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
